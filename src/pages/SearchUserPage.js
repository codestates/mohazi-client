import { withRouter, Route, useHistory } from "react-router-dom";

function SearchUserPage() {
    const history = useHistory();
    return (
        <div> SearchUserPage </div>
    )
}

export default withRouter(SearchUserPage);