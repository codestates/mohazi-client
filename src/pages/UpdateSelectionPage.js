import { withRouter, Route, useHistory } from "react-router-dom";

function UpdateSelectionPage() {
    const history = useHistory();
    return (
        <div> UpdateSelectionPage </div>
    )
}

export default withRouter(UpdateSelectionPage);