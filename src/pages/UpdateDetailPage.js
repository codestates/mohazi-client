import { withRouter, Route, useHistory } from "react-router-dom";

function UpdateDetailPage() {
    const history = useHistory();
    return (
        <div> UpdateDetailPage </div>
    )
}

export default withRouter(UpdateDetailPage);